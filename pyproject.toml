[project]
name = "arm-kb-search"
version = "0.4.0"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "rank-bm25",
  "sentence-transformers>=6.0.0",
  "usearch==2.26.0",
  "numkong==7.8.0",
]

[build-system]
requires = ["setuptools>=84.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["arm_kb_search*"]
